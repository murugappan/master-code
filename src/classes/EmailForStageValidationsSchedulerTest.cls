@isTest
private class EmailForStageValidationsSchedulerTest {
    /* commenting out 
    static testMethod void myUnitTest() {
    test.startTest();
    EmailForStageValidationsScheduler nightlyUpdate = new EmailForStageValidationsScheduler();
    String schedule = '0 0 23 * * ?';
    system.schedule('Nightly Update', schedule, nightlyUpdate);
    test.stopTest();
    }
    commenting out */
}