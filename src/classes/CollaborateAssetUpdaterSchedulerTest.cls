@isTest
private class  CollaborateAssetUpdaterSchedulerTest {
    /* commented out 
    static testMethod void myUnitTest() {
    test.startTest();
     CollaborateAssetUpdaterScheduler nightlyUpdate = new  CollaborateAssetUpdaterScheduler();
    String schedule = '0 0 23 * * ?';
    system.schedule('Nightly Update', schedule, nightlyUpdate);
    test.stopTest();
    }
    commented out */
}